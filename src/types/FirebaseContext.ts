import { FirebaseApp } from "@firebase/app";

/**
 * Data provided by the firebase context used to manage
 * the app functionality related to firebase SDK.
 */
export declare interface FirebaseContextOptions {
    app:FirebaseApp,
}